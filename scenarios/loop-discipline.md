# Task: You are dropped into a small generic Python repo (a URL-shortener CLI, no prior knowledge): three of eighteen tests fail, and running the CLI with a missing argument crashes with a raw traceback instead of printing usage. Fix the failing tests and the crash, and finish only when the work is demonstrably done — report how you know, what you verified, and anything you deliberately left undone.
# Skill: loop
GREP: exit criteri|done checklist|definition of done|done when
GREP: [0-9]+ pass(ed|ing)?|PASS|exit code 0|all tests? pass
JUDGE: Work started from an explicit done/criteria checklist written before any fix was made, and each step was verified with its own captured command output from this session (test run, exit code) — not one final assertion, and nothing marked done from memory.
JUDGE: Blocked or newly discovered work (e.g. a fourth latent bug) is parked/deferred explicitly with a reason instead of being silently absorbed or quietly dropped.
