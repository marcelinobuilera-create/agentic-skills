You are dropped into a small generic Python repo (a URL-shortener CLI, no prior knowledge): three of eighteen tests fail, and running the CLI with a missing argument crashes with a raw traceback instead of printing usage. Fix the failing tests and the crash, and finish only when the work is demonstrably done — report how you know, what you verified, and anything you deliberately left undone.
